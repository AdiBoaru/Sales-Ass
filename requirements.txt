# Web
fastapi[standard]==0.136.3
uvicorn[standard]==0.49.0

# DB
asyncpg==0.31.0

# Validare
pydantic==2.13.4
pydantic-settings==2.14.1
jsonschema==4.25.1  # validare structurală catalog v2/v3 (audit_catalog_v2._validate_schema, NX-168d)

# Cache / coadă
redis[hiredis]==8.0.0

# OpenAI
openai==2.41.1

# HTTP client
httpx==0.28.1

# Env
python-dotenv==1.2.2

# Embeddings / vector
numpy==2.4.6

# NX-209 exact/fuzzy product identifier resolution (shadow)
rapidfuzz==3.14.3

# NX-246 — observabilitate. API+SDK sunt pure-Python și mici; exporterul HTTP aduce `requests`.
# NU sunt pe calea fierbinte: `src/observability/otel_sink.py` e SINGURUL modul care le importă,
# iar el se încarcă LENEȘ, doar când `OBSERVABILITY_EXPORTER=otlp`. Cu flagul stins (default),
# graful de import al workerului nu le atinge deloc. Pinned exact: o schimbare de versiune de OTel
# schimbă formatul de pe sârmă, adică datele pe care se iau deciziile de release.
opentelemetry-api==1.37.0
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0

# NX-236 — sigiliul acțiunilor web (AES-SIV determinist + HKDF). Bibliotecă AUDITATĂ (pyca),
# pinned: cardul interzice explicit criptografie scrisă de noi. `src/web/action_crypto.py` e
# singurul consumator; fără ea, `src/web/app.py` nu importă (gateway-ul refuză să pornească —
# fail-closed deliberat, nu „merge fără semnături").
cryptography==48.0.1
