<!-- GENERAT de scripts/state_writers.py -- nu edita de mână. -->
<!-- Rulează `python scripts/state_writers.py` ca să-l regenerezi. -->

# Inventarul scriitorilor de stare (NX-327, pasul 0.5 al kernelului)

Fiecare loc din `src/**/*.py` care scrie starea conversației (v1 sau v2), derivat MECANIC prin `ast` (nu grep). Pregătește I3/I20 din [`docs/KERNEL-CONTRACT-v1.md`](KERNEL-CONTRACT-v1.md): pasul 3 nu poate face din reducer singurul scriitor peste o stare pe care o mai scriu și alții, fără să știe cine sunt aceia.

**Totaluri:** 106 scriitori găsiți, 73 intrări de soartă declarate, 3 `unresolved`.
- `becomes_proposal`: 18
- `executor_output`: 5
- `retired`: 6
- `stays`: 77

## `needs_topic` -- nevoi + subiect/raft

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L210) | v1 | `subscript_assign` | `constraints` | Acelasi dual-write: `ctx.state.constraints[field] = answer` (v1) langa `StateUpdateProposal("set_need", key=field, value=answer, ...)` (v2), scrise UNA langa alta. Sursa e solida (raspuns la o intrebare cu `question_id` verificat -- `user_explicit` legitim), doar mecanismul de scriere e dublu. Jumatatea v1 devine inutila odata ce reducerul e singurul scriitor. | `becomes_proposal` |
| `src/worker/processor.py:_build_new_state` (L452) | v1 | `dict_literal` | `constraints` | Scrie `constraints`/`search_constraints` in dict-ul persistat v1, din ce a mutat deja IN-PLACE `agent_stage`/`clarify_resume_stage` pe `ctx.state`. Aceleasi stagii populeaza `ctx.state_proposals` (set_need/set_topic), consumate de `_build_state_v2`. E write-back-ul v1, nu sursa -- dispare odata cu retragerea formei v1. | `becomes_proposal` |
| `src/worker/processor.py:_build_new_state` (L452) | v1 | `dict_literal` | `search_constraints` | Scrie `constraints`/`search_constraints` in dict-ul persistat v1, din ce a mutat deja IN-PLACE `agent_stage`/`clarify_resume_stage` pe `ctx.state`. Aceleasi stagii populeaza `ctx.state_proposals` (set_need/set_topic), consumate de `_build_state_v2`. E write-back-ul v1, nu sursa -- dispare odata cu retragerea formei v1. | `becomes_proposal` |
| `src/worker/stages/agent.py:_learn_constraints` (L804) | v1 | `assign` | `search_constraints` | Scrie stiva `search_constraints` din ce a CAUTAT agentul (`observed_constraints`), nu dintr-o interpretare a mesajului -- exact mecanismul pe care cardul il declara `retired` explicit (NX-297). I20 cere ca nevoile/subiectul sa fie o functie PURA a interpretarii turului; un semnal derivat din argumentele de tool e un ocolis pe care kernelul il inchide. Dispare cand pasul 5 (turn_interpreter) preia sursa nevoilor. | `retired` |
| `src/worker/stages/agent.py:_learn_subject` (L867) | v1 | `assign` | `search_constraints` | Aceeasi familie NX-314: deriva subiectul din `run.retrieved` (ce a adus cautarea), nu din ce a spus clientul. Chemata EXCLUSIV din `_learn_constraints` (deja `retired`); scrie AMBELE forme (`ctx.state.search_constraints` direct + `StateUpdateProposal("set_topic")`), dar proveniet dintr-un semnal de executie, nu de interpretare (I20) -- propunerea trece mecanic prin reducer, dar sursa ei e exact ce pasul 5 inlocuieste. | `retired` |
| `src/worker/stages/agent.py:agent_stage` (L996) | v1 | `assign` | `search_constraints` | Punctul unde rezultatul lui `merge_constraints` (explicit `retired` in card) chiar ATERIZEAZA pe stare: `ctx.state.search_constraints = merged_constraints`. Cu v2 pornit, `_v2_constraints` poate suprascrie preview-ul, dar scrierea-v1 ramane owner-ul de fapt pentru cititorii legacy (`_filters_hint`, `state_block`). Dispare cand v2 devine singura forma persistata si acesti cititori trec pe `state_v2`. | `retired` |
| `src/worker/stages/clarify.py:clarify_resume_stage` (L121) | v1 | `subscript_assign` | `constraints` | `ctx.state.constraints[field] = answer` (v1, linia 121) scrisa CHIAR INAINTE de `_propose_resume(ctx, field, answer, pq)` (v2, `set_need`), din ACELASI raspuns al clientului la o clarificare -- sursa e `user_explicit` legitima, doar mecanismul de scriere e dublu. Jumatatea v1 devine inutila cand reducerul ramane singurul scriitor. | `becomes_proposal` |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L221) | v2 | `proposal_constructor` | `set_need` | Acelasi dual-write: `ctx.state.constraints[field] = answer` (v1) langa `StateUpdateProposal("set_need", key=field, value=answer, ...)` (v2), scrise UNA langa alta. Sursa e solida (raspuns la o intrebare cu `question_id` verificat -- `user_explicit` legitim), doar mecanismul de scriere e dublu. Jumatatea v1 devine inutila odata ce reducerul e singurul scriitor. | `becomes_proposal` |
| `src/agent/action_kernel.py:dispatch` (L341) | v2 | `proposal_call` | `_handle_answer_clarification() -> set_need` | Idem, pentru ramura `set_need` din `_handle_answer_clarification`. | `stays` |
| `src/agent/brain.py:_state_proposals_from_plan` (L937) | v2 | `proposal_constructor` | `set_topic` | Puntea planului creierului unic (`AnswerPlanV2.state_update_proposals`) spre reducer -- sursa e deja planul MODELULUI, validat de `to_v1()`/grounding inainte sa ajunga aici, iar iesirea e deja `StateUpdateProposal`. Ramura cu `op` dinamic (necunoscut static) e listata separat in `unresolved`; ramura `set_topic` (literala) e deja pe forma tinta. | `stays` |
| `src/agent/brain.py:run_main_brain` (L1458) | v2 | `proposal_call` | `_state_proposals_from_plan() -> set_topic` | Apel indirect catre `_state_proposals_from_plan` -- deja proposal-based. | `stays` |
| `src/conversation/state_reducer.py:_handle_confirm` (L430) | v2 | `dataclasses_replace` | `needs` | Handler intern al reducerului (NX-235) -- `state_reducer.py` E deja implementarea invariantei I3 ("SINGURUL loc care are voie sa schimbe starea", cf. docstring-ul modulului). Pasul 3 al kernelului EXTINDE acest modul (provenance checker, delta mapper, park/resume), nu il inlocuieste -- fiecare handler ramane exact aici. | `stays` |
| `src/conversation/state_reducer.py:_handle_revoke` (L482) | v2 | `dataclasses_replace` | `needs` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_revoke` (L482) | v2 | `dataclasses_replace` | `revocations` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_need` (L360) | v2 | `dataclasses_replace` | `needs` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_need` (L360) | v2 | `dataclasses_replace` | `revocations` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_need` (L387) | v2 | `dataclasses_replace` | `needs` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_need` (L387) | v2 | `dataclasses_replace` | `revocations` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L510) | v2 | `dataclasses_replace` | `topic` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L523) | v2 | `dataclasses_replace` | `topic` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L552) | v2 | `dataclasses_replace` | `topic` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L552) | v2 | `dataclasses_replace` | `needs` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L552) | v2 | `dataclasses_replace` | `revocations` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_v2.py:enforce_caps` (L791) | v2 | `dataclasses_replace` | `needs` | Acelasi mecanism de degradare -- vezi active_search. | `stays` |
| `src/conversation/state_v2.py:enforce_caps` (L791) | v2 | `dataclasses_replace` | `revocations` | Acelasi mecanism de degradare -- vezi active_search. | `stays` |
| `src/conversation/state_v2.py:serialize` (L826) | v2 | `dataclasses_replace` | `revocations` | Acelasi `shrink`: nevoile non-hard/non-sensibile sunt primele taiate. | `stays` |
| `src/conversation/state_v2.py:serialize` (L827) | v2 | `dataclasses_replace` | `needs` | Acelasi `shrink`: nevoile non-hard/non-sensibile sunt primele taiate. | `stays` |
| `src/worker/stages/agent.py:_filter_proposals` (L254) | v2 | `proposal_call` | `_need_proposals() -> set_need` | Traduce `RouteDecision.filters`/`category_key` in propuneri typed -- deja proposal-based (v2), fara nicio scriere v1 in ACEEASI functie de retras. (Observatie separata, nu schimba verdictul: `route.filters` ramane gol pe majoritatea turelor post-NX-297, caile care il populeaza azi fiind doar `alias.py` -- deci codul e corect, dar rar exercitat.) | `stays` |
| `src/worker/stages/agent.py:_filter_proposals` (L247) | v2 | `proposal_constructor` | `set_topic` | Traduce `RouteDecision.filters`/`category_key` in propuneri typed -- deja proposal-based (v2), fara nicio scriere v1 in ACEEASI functie de retras. (Observatie separata, nu schimba verdictul: `route.filters` ramane gol pe majoritatea turelor post-NX-297, caile care il populeaza azi fiind doar `alias.py` -- deci codul e corect, dar rar exercitat.) | `stays` |
| `src/worker/stages/agent.py:_learn_constraints` (L805) | v2 | `proposal_call` | `_need_proposals() -> set_need` | Scrie stiva `search_constraints` din ce a CAUTAT agentul (`observed_constraints`), nu dintr-o interpretare a mesajului -- exact mecanismul pe care cardul il declara `retired` explicit (NX-297). I20 cere ca nevoile/subiectul sa fie o functie PURA a interpretarii turului; un semnal derivat din argumentele de tool e un ocolis pe care kernelul il inchide. Dispare cand pasul 5 (turn_interpreter) preia sursa nevoilor. | `retired` |
| `src/worker/stages/agent.py:_learn_constraints` (L815) | v2 | `proposal_call` | `_learn_subject() -> set_topic` | Scrie stiva `search_constraints` din ce a CAUTAT agentul (`observed_constraints`), nu dintr-o interpretare a mesajului -- exact mecanismul pe care cardul il declara `retired` explicit (NX-297). I20 cere ca nevoile/subiectul sa fie o functie PURA a interpretarii turului; un semnal derivat din argumentele de tool e un ocolis pe care kernelul il inchide. Dispare cand pasul 5 (turn_interpreter) preia sursa nevoilor. | `retired` |
| `src/worker/stages/agent.py:_learn_subject` (L876) | v2 | `proposal_constructor` | `set_topic` | Aceeasi familie NX-314: deriva subiectul din `run.retrieved` (ce a adus cautarea), nu din ce a spus clientul. Chemata EXCLUSIV din `_learn_constraints` (deja `retired`); scrie AMBELE forme (`ctx.state.search_constraints` direct + `StateUpdateProposal("set_topic")`), dar proveniet dintr-un semnal de executie, nu de interpretare (I20) -- propunerea trece mecanic prin reducer, dar sursa ei e exact ce pasul 5 inlocuieste. | `retired` |
| `src/worker/stages/agent.py:_need_proposals` (L271) | v2 | `proposal_constructor` | `set_need` | Traducator PUR, agnostic de sursa: `dict de sloturi -> StateUpdateProposal(set_need)`. Chemat atat din `_filter_proposals` (stays) cat si din `_learn_constraints` (retired) -- functia insasi nu presupune nimic despre provenienta, deci ramane utila si dupa ce pasul 5 schimba sursa reala a sloturilor. | `stays` |
| `src/worker/stages/agent.py:_need_proposals` (L277) | v2 | `proposal_constructor` | `set_need` | Traducator PUR, agnostic de sursa: `dict de sloturi -> StateUpdateProposal(set_need)`. Chemat atat din `_filter_proposals` (stays) cat si din `_learn_constraints` (retired) -- functia insasi nu presupune nimic despre provenienta, deci ramane utila si dupa ce pasul 5 schimba sursa reala a sloturilor. | `stays` |
| `src/worker/stages/agent.py:_v2_constraints` (L300) | v2 | `proposal_call` | `_filter_proposals() -> set_topic` | Previzualizare PURA: ruleaza `_filter_proposals` + `reduce_all` peste starea curenta, ca sa dea un rezultat identic cu ce se va persista la commit (docstring: "nu poate diverge de adevar, e aceeasi functie rulata mai devreme"). Deja aliniat cu directia kernelului. | `stays` |
| `src/worker/stages/clarify.py:_propose_resume` (L175) | v2 | `proposal_constructor` | `set_need` | Construieste `StateUpdateProposal("set_need", ...)` din raspunsul la clarificare -- deja forma tinta (sursa `user_explicit`, legitima). | `stays` |
| `src/worker/stages/clarify.py:clarify_resume_stage` (L125) | v2 | `proposal_call` | `_propose_resume() -> set_need` | `ctx.state.constraints[field] = answer` (v1, linia 121) scrisa CHIAR INAINTE de `_propose_resume(ctx, field, answer, pq)` (v2, `set_need`), din ACELASI raspuns al clientului la o clarificare -- sursa e `user_explicit` legitima, doar mecanismul de scriere e dublu. Jumatatea v1 devine inutila cand reducerul ramane singurul scriitor. | `becomes_proposal` |

## `focus` -- focus (produsul selectat)

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/agent/action_kernel.py:_handle_product_action` (L158) | v2 | `proposal_call` | `_select() -> set_references.selected_product` | Apel indirect catre `_select` (vezi mai jos) -- acelasi verdict: mecanism v2-only, nimic de mutat. | `stays` |
| `src/agent/action_kernel.py:_select` (L133) | v2 | `proposal_constructor` | `set_references.selected_product` | O actiune (click) e o selectie EXPLICITA (`source="action"`) -- trece deja prin reducer ca orice alta propunere (`set_references`, payload `selected_product`). Nu exista o forma v1 a acestui concept (v1 nu are `selected_product`), deci nu e nimic de migrat. | `stays` |
| `src/agent/deterministic.py:_handle_detail_intent` (L493) | v2 | `proposal_call` | `_resolve_anchor() -> set_references.selected_product` | Apel indirect catre `_resolve_anchor` -- vezi mai jos, mecanism v2-only. | `stays` |
| `src/agent/deterministic.py:_handle_review_intent` (L354) | v2 | `proposal_call` | `_resolve_anchor() -> set_references.selected_product` | Idem, acelasi apel indirect catre `_resolve_anchor`. | `stays` |
| `src/agent/deterministic.py:_resolve_anchor` (L235) | v2 | `proposal_constructor` | `set_references.selected_product` | Rezolvarea de referinte v2 (`reference_precedence_v2_enabled`, NX-235/236): o referinta REZOLVATA explicit e memorata prin `StateUpdateProposal("set_references", payload={"selected_product":...})`. Conceptul de "produs selectat" nu exista in v1 -- nimic de migrat, mecanismul e nascut deja pe forma tinta. | `stays` |

## `active_search` -- sesiune de cautare

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/evals/golden.py:advance_turn` (L365) | v1 | `assign` | `active_search` | Harness de teste (`ScriptedLLM`/golden): re-implementeaza DELIBERAT merge-ul lui `processor._build_new_state`/`_turn_proposals` ca sa avanseze starea intre ture scriptate, fara DB/worker real. Nu ruleaza in pipeline-ul de productie -- invariantele I3/I20 vizeaza pipeline-ul, nu dublura de test; daca productia isi schimba mecanismul de scriere, harness-ul se actualizeaza separat, dar nu e el insusi o tinta a pasului 3. | `stays` |
| `src/tools/catalog_tools.py:continue_search_session` (L1490) | v1 | `patch_subscript` | `active_search` | Tool-ul de paginare scrie `ctx.state_patch["active_search"]` cu cursorul/pagina noua -- rezultatul executiei unei cautari, nu o interpretare a mesajului. I20 permite explicit `active_search` ca iesire de executor. | `executor_output` |
| `src/tools/catalog_tools.py:search_products_tool` (L2495) | v1 | `patch_mutating_call` | `active_search` | Tool-ul de cautare scrie/sterge `ctx.state_patch["active_search"]` (sesiunea de paginare) ca parte din executia cautarii -- iesire de executor, permisa explicit de I20. | `executor_output` |
| `src/tools/catalog_tools.py:search_products_tool` (L2306) | v1 | `patch_subscript` | `active_search` | Tool-ul de cautare scrie/sterge `ctx.state_patch["active_search"]` (sesiunea de paginare) ca parte din executia cautarii -- iesire de executor, permisa explicit de I20. | `executor_output` |
| `src/worker/processor.py:_build_new_state` (L465) | v1 | `dict_literal` | `active_search` | `_turn_proposals` (in ACELASI fisier) construieste deja `StateUpdateProposal("set_active_search", ...)` din EXACT aceleasi surse (`ctx.state_patch["active_search"]` sau `None` cand turul nu e o cautare) -- docstringul functiei o spune explicit: "aceleasi delte pe care _build_new_state le scrie in v1, doar exprimate in vocabularul reducerului". Scrierea-dict-literal v1 devine redundanta cand v2 e autoritar. | `becomes_proposal` |
| `src/conversation/state_reducer.py:_handle_set_active_search` (L671) | v2 | `dataclasses_replace` | `active_search` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_v2.py:enforce_caps` (L791) | v2 | `dataclasses_replace` | `active_search` | Degradarea de buget (I17): taie in ordinea declarata (referinte afisate -> intrebari -> tombstone-uri -> nevoi soft -> sesiune/cos), NICIODATA hard/sensibile. Mecanismul de buget al reducerului -- pasul 3 il extinde (thresholds), nu-l muta. | `stays` |
| `src/conversation/state_v2.py:serialize` (L830) | v2 | `dataclasses_replace` | `active_search` | Lista de `shrink` din `serialize()` (I17): taie `active_search`/`cart_ref` cand starea depaseste bugetul de 6KB -- ultimele sacrificate, dupa nevoi soft. Mecanism al reducerului, nu se muta. | `stays` |
| `src/worker/processor.py:_build_state_v2` (L352) | v2 | `proposal_call` | `_turn_proposals() -> set_active_search` | Constructia stariii v2 la commit: reduce propunerile turului (inclusiv `_turn_proposals`) peste starea hidratata proaspat -- funcția PURA care implementeaza "retry re-aplica ACELEASI propuneri" (P6). Chiar mecanismul pe care pasul 3 il extinde. | `stays` |
| `src/worker/processor.py:_turn_proposals` (L314) | v2 | `proposal_constructor` | `set_active_search` | "Deltele turului, ca PROPUNERI typed -- aceleasi delte pe care _build_new_state le scrie in v1, doar exprimate in vocabularul reducerului" (docstring-ul functiei). Exact forma pe care pasul 3 o vrea peste tot -- Sender-ul (P3) emite deja propuneri, nu scrie direct. | `stays` |
| `src/worker/processor.py:_turn_proposals` (L319) | v2 | `proposal_constructor` | `set_active_search` | "Deltele turului, ca PROPUNERI typed -- aceleasi delte pe care _build_new_state le scrie in v1, doar exprimate in vocabularul reducerului" (docstring-ul functiei). Exact forma pe care pasul 3 o vrea peste tot -- Sender-ul (P3) emite deja propuneri, nu scrie direct. | `stays` |

## `references` -- referinte

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/agent/brain.py:_remember_offered` (L402) | v1 | `patch_subscript` | `offered_chips` | Scrie `state_patch["offered_chips"]` -- camp fara slot in `ConversationStateV2.References` (nu e in tabelul cardului), deci ramane pass-through pe AMBELE forme de stare; nu e o migrare de facut, doar o clasa de camp diferita (P3, nu I3/I20 al kernelului). | `stays` |
| `src/agent/finalize.py:_apply_move_chips` (L867) | v1 | `patch_subscript` | `offered_chips` | Acelasi camp `offered_chips` ca `_remember_offered` (brain.py) -- pass-through fara slot in reducer, nu e in domeniul I3/I20 al kernelului. | `stays` |
| `src/evals/golden.py:advance_turn` (L360) | v1 | `assign` | `displayed_products` | Acelasi harness -- vezi active_search. | `stays` |
| `src/worker/processor.py:_build_new_state` (L439) | v1 | `dict_literal` | `displayed_products` | `_turn_proposals` (in ACELASI fisier) produce deja `StateUpdateProposal("set_references", payload={"displayed_products": ...})` din EXACT aceleasi surse (`ctx.reply.products` / `ctx.state_patch["displayed_products"]`) -- docstringul o declara textual. Dict-ul-literal v1 e redundant cand v2 e autoritar. | `becomes_proposal` |
| `src/worker/stages/agent.py:_prune_displayed` (L676) | v1 | `assign` | `displayed_products` | Scoate din `displayed_products` produsele devenite contraindicate cand un context de siguranta (NX-173) tocmai a devenit activ -- un semnal DETERMINIST, derivat din catalog + politica, nu din interpretarea mesajului. Exact clasa pe care I20 o declara permisa ca iesire de executor ("referinte"): kernelul nu muta asta, doar o recunoaste ca atare. | `executor_output` |
| `src/worker/stages/agent.py:_prune_displayed` (L677) | v1 | `patch_subscript` | `displayed_products` | Scoate din `displayed_products` produsele devenite contraindicate cand un context de siguranta (NX-173) tocmai a devenit activ -- un semnal DETERMINIST, derivat din catalog + politica, nu din interpretarea mesajului. Exact clasa pe care I20 o declara permisa ca iesire de executor ("referinte"): kernelul nu muta asta, doar o recunoaste ca atare. | `executor_output` |
| `src/agent/action_kernel.py:_handle_compare` (L168) | v2 | `proposal_constructor` | `set_references.compared_products` | `StateUpdateProposal("set_references", payload={compared_products,last_action})` dupa o comparatie declansata de buton -- deja proposal-based (v2), fara nicio scriere v1 echivalenta de retras. Nu exista mecanism v1 de "produse comparate". | `stays` |
| `src/agent/action_kernel.py:_handle_compare` (L168) | v2 | `proposal_constructor` | `set_references.last_action` | `StateUpdateProposal("set_references", payload={compared_products,last_action})` dupa o comparatie declansata de buton -- deja proposal-based (v2), fara nicio scriere v1 echivalenta de retras. Nu exista mecanism v1 de "produse comparate". | `stays` |
| `src/agent/action_kernel.py:_handle_product_action` (L158) | v2 | `proposal_call` | `_select() -> set_references.last_action` | Apel indirect catre `_select`, jumatatea `last_action` -- v2-only (vezi `_select`). | `stays` |
| `src/agent/action_kernel.py:_select` (L133) | v2 | `proposal_constructor` | `set_references.last_action` | Aceeasi propunere, jumatatea `last_action` -- concept introdus direct pe v2 (NX-236), fara corespondent v1. | `stays` |
| `src/agent/action_kernel.py:dispatch` (L337) | v2 | `proposal_call` | `_handle_compare() -> set_references.compared_products` | Idem, pentru ramurile `_handle_compare`/`_select` (ambele deja v2-only). | `stays` |
| `src/agent/action_kernel.py:dispatch` (L337) | v2 | `proposal_call` | `_handle_compare() -> set_references.last_action` | Idem, pentru ramurile `_handle_compare`/`_select` (ambele deja v2-only). | `stays` |
| `src/conversation/state_reducer.py:_handle_set_references` (L652) | v2 | `dataclasses_replace` | `references` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_v2.py:enforce_caps` (L791) | v2 | `dataclasses_replace` | `references` | Acelasi mecanism de degradare -- vezi active_search. | `stays` |
| `src/conversation/state_v2.py:serialize` (L824) | v2 | `dataclasses_replace` | `references` | Acelasi `shrink`: `displayed_products` sunt PRIMELE taiate (valoarea lor de afisare scade cel mai repede) -- mecanism de buget al reducerului. | `stays` |
| `src/worker/processor.py:_build_state_v2` (L352) | v2 | `proposal_call` | `_turn_proposals() -> set_references.displayed_products` | Acelasi `_build_state_v2` -- reduce propunerile de `references` (din `_turn_proposals`) peste starea hidratata. Parte din mecanismul-tinta. | `stays` |
| `src/worker/processor.py:_turn_proposals` (L303) | v2 | `proposal_constructor` | `set_references.displayed_products` | Acelasi `_turn_proposals`, jumatatea `displayed_products` -- vezi active_search. | `stays` |
| `src/worker/processor.py:_turn_proposals` (L325) | v2 | `proposal_constructor` | `set_references.displayed_products` | Acelasi `_turn_proposals`, jumatatea `displayed_products` -- vezi active_search. | `stays` |

## `clarification` -- clarificare

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L229) | v1 | `assign` | `pending_question` | Dual-write: `ctx.state.pending_question = None` + `ctx.state.asked_intents.append(...)` (v1, direct) ALATURI de `StateUpdateProposal("resolve_question", ...)` (v2), in ACEEASI functie. Jumatatea v1 e deja redundanta -- propunerea are toata informatia. Cand reducerul devine singurul scriitor (I3), scrierea directa dispare si ramane doar propunerea deja emisa aici. | `becomes_proposal` |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L226) | v1 | `mutating_call` | `asked_intents` | Dual-write: `ctx.state.pending_question = None` + `ctx.state.asked_intents.append(...)` (v1, direct) ALATURI de `StateUpdateProposal("resolve_question", ...)` (v2), in ACEEASI functie. Jumatatea v1 e deja redundanta -- propunerea are toata informatia. Cand reducerul devine singurul scriitor (I3), scrierea directa dispare si ramane doar propunerea deja emisa aici. | `becomes_proposal` |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L227) | v1 | `subscript_assign` | `asked_intents` | Dual-write: `ctx.state.pending_question = None` + `ctx.state.asked_intents.append(...)` (v1, direct) ALATURI de `StateUpdateProposal("resolve_question", ...)` (v2), in ACEEASI functie. Jumatatea v1 e deja redundanta -- propunerea are toata informatia. Cand reducerul devine singurul scriitor (I3), scrierea directa dispare si ramane doar propunerea deja emisa aici. | `becomes_proposal` |
| `src/agent/finalize.py:_apply_turn_shape` (L614) | v1 | `subscript_assign` | `asked_intents` | `ctx.state.asked_intents[:] = [*asked, offer.facet][-8:]` marcheaza fateta intrebarii de ingustare (NX-315) ca deja-intrebata -- SINGURA scriere pe acest drum, fara nicio propunere v2 echivalenta azi (nu exista `resolve_question`/`set_pending_question` emisa de aici). Spre deosebire de `becomes_proposal` din clarify.py (unde propunerea deja exista alaturi), aici I3 cere ca pasul 3 sa ADAUGE o propunere noua pentru fapta asta -- nu doar sa retraga jumatatea v1 a unui dual-write existent, ci sa creeze mecanismul. | `becomes_proposal` |
| `src/evals/golden.py:advance_turn` (L361) | v1 | `assign` | `pending_question` | Acelasi harness -- vezi active_search. | `stays` |
| `src/worker/processor.py:_build_new_state` (L445) | v1 | `dict_literal` | `pending_question` | Scrie `pending_question`/`asked_intents` in dict-ul persistat v1, din ce au mutat deja IN-PLACE alte stagii (clarify/action_kernel) pe `ctx.state`. Acele stagii emit deja propuneri echivalente (`resolve_question`/`set_pending_question`) in `ctx.state_proposals`, consumate de `_build_state_v2` -> `reduce_all`. Scrierea v1 e write-back-ul legacy, nu o sursa noua de adevar -- dispare cand v2 e singura forma persistata. | `becomes_proposal` |
| `src/worker/processor.py:_build_new_state` (L452) | v1 | `dict_literal` | `asked_intents` | Scrie `pending_question`/`asked_intents` in dict-ul persistat v1, din ce au mutat deja IN-PLACE alte stagii (clarify/action_kernel) pe `ctx.state`. Acele stagii emit deja propuneri echivalente (`resolve_question`/`set_pending_question`) in `ctx.state_proposals`, consumate de `_build_state_v2` -> `reduce_all`. Scrierea v1 e write-back-ul legacy, nu o sursa noua de adevar -- dispare cand v2 e singura forma persistata. | `becomes_proposal` |
| `src/worker/stages/clarify.py:clarify_resume_stage` (L129) | v1 | `mutating_call` | `asked_intents` | Acelasi tipar ca la `_handle_answer_clarification`: `ctx.state.asked_intents.append(...)` (v1) langa apelul la `_propose_resume` (v2, `resolve_question`). Cand v2 devine autoritar, scrierea v1 e cea care dispare -- propunerea are deja tot ce trebuie. | `becomes_proposal` |
| `src/worker/stages/clarify.py:clarify_resume_stage` (L130) | v1 | `subscript_assign` | `asked_intents` | Acelasi tipar ca la `_handle_answer_clarification`: `ctx.state.asked_intents.append(...)` (v1) langa apelul la `_propose_resume` (v2, `resolve_question`). Cand v2 devine autoritar, scrierea v1 e cea care dispare -- propunerea are deja tot ce trebuie. | `becomes_proposal` |
| `src/agent/action_kernel.py:_handle_answer_clarification` (L212) | v2 | `proposal_constructor` | `resolve_question` | Dual-write: `ctx.state.pending_question = None` + `ctx.state.asked_intents.append(...)` (v1, direct) ALATURI de `StateUpdateProposal("resolve_question", ...)` (v2), in ACEEASI functie. Jumatatea v1 e deja redundanta -- propunerea are toata informatia. Cand reducerul devine singurul scriitor (I3), scrierea directa dispare si ramane doar propunerea deja emisa aici. | `becomes_proposal` |
| `src/agent/action_kernel.py:dispatch` (L341) | v2 | `proposal_call` | `_handle_answer_clarification() -> resolve_question` | Router-ul actiunilor -- apeleaza handlerul potrivit (`_handle_answer_clarification`); verdictul de fond e la handler (`becomes_proposal`), `dispatch` insusi ramane doar cablajul. | `stays` |
| `src/agent/brain.py:_persist_clarification` (L1007) | v2 | `proposal_constructor` | `set_pending_question` | Pe calea creierului unic (NX-239, flag OFF), clarificarea e NATIV v2: emite doar `StateUpdateProposal("set_pending_question", ...)`, fara nicio scriere v1 in aceeasi functie. Nimic de migrat -- e deja forma tinta. | `stays` |
| `src/agent/brain.py:run_main_brain` (L1555) | v2 | `proposal_call` | `_persist_clarification() -> set_pending_question` | Apel indirect catre `_persist_clarification` -- deja v2-only (vezi mai sus). | `stays` |
| `src/conversation/state_reducer.py:_handle_resolve_question` (L614) | v2 | `dataclasses_replace` | `pending_clarification` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_resolve_question` (L614) | v2 | `dataclasses_replace` | `asked_questions` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_pending_question` (L596) | v2 | `dataclasses_replace` | `pending_clarification` | Handler intern al reducerului -- vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L552) | v2 | `dataclasses_replace` | `pending_clarification` | Efect colateral al schimbarii de subiect: cere clarificare noua (`pending_clarification`=None la topic reset) -- tot in interiorul reducerului, vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_topic` (L552) | v2 | `dataclasses_replace` | `asked_questions` | Efect colateral al schimbarii de subiect: cere clarificare noua (`pending_clarification`=None la topic reset) -- tot in interiorul reducerului, vezi `_handle_confirm`. | `stays` |
| `src/conversation/state_v2.py:enforce_caps` (L791) | v2 | `dataclasses_replace` | `asked_questions` | Acelasi mecanism de degradare -- vezi active_search. | `stays` |
| `src/conversation/state_v2.py:serialize` (L825) | v2 | `dataclasses_replace` | `asked_questions` | Acelasi `shrink`: `asked_questions` taiate la ultimele 3 cand bugetul e depasit. | `stays` |
| `src/worker/stages/clarify.py:_propose_resume` (L166) | v2 | `proposal_constructor` | `resolve_question` | Construieste `StateUpdateProposal("resolve_question", ...)` -- deja forma tinta. | `stays` |
| `src/worker/stages/clarify.py:clarify_resume_stage` (L125) | v2 | `proposal_call` | `_propose_resume() -> resolve_question` | Acelasi tipar ca la `_handle_answer_clarification`: `ctx.state.asked_intents.append(...)` (v1) langa apelul la `_propose_resume` (v2, `resolve_question`). Cand v2 devine autoritar, scrierea v1 e cea care dispare -- propunerea are deja tot ce trebuie. | `becomes_proposal` |

## `cart` -- cos (in afara contractului kernelului -- Out of Scope permite adaugarea in JSON)

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/agent/action_kernel.py:_handle_commerce` (L318) | v1 | `patch_subscript` | `cart_ref` | `cart_ref` scris prin `state_patch` dupa o mutatie de cos (NX-237, CartService) -- starea coșului e explicit AFARA din contractul kernelului (Out of Scope al cardului); ramane owner-ul ei propriu. | `stays` |
| `src/evals/golden.py:advance_turn` (L367) | v1 | `assign` | `cart` | Acelasi harness -- vezi active_search. `cart` oricum in afara contractului kernelului. | `stays` |
| `src/tools/commerce_tools.py:_cart_add_v2` (L360) | v1 | `tool_result_kwarg` | `cart_ref` | `ToolResult(state_patch={"cart_ref":...})` dupa o mutatie prin CartService (NX-237) -- coșul e explicit AFARA din contractul kernelului (Out of Scope al cardului). | `stays` |
| `src/tools/commerce_tools.py:cart_add_tool` (L314) | v1 | `tool_result_kwarg` | `cart` | Calea LEGACY (pre-CartService) a lui `cart_add`, `state_patch={"cart":cart}` -- tot coș, tot in afara contractului kernelului. | `stays` |
| `src/conversation/state_reducer.py:_handle_set_cart_ref` (L691) | v2 | `dataclasses_replace` | `cart_ref` | Handler intern al reducerului pentru `cart_ref` -- coșul ramane in afara contractului kernelului (Out of Scope), dar reducerul insusi nu se muta. | `stays` |
| `src/conversation/state_v2.py:serialize` (L830) | v2 | `dataclasses_replace` | `cart_ref` | Acelasi `shrink`, jumatatea `cart_ref` -- vezi active_search. `cart` ramane oricum in afara contractului kernelului. | `stays` |
| `src/worker/processor.py:_build_state_v2` (L376) | v2 | `dataclasses_replace` | `cart_ref` | Acelasi `_build_state_v2`: `cart_ref` cărat din `ctx.state_patch` direct in `ConversationStateV2` (NX-237 il modeleaza explicit, spre deosebire de `cart`/`safety` care raman passthrough) -- coșul e oricum in afara contractului kernelului. | `stays` |

## `safety` -- context de siguranta (NX-173, P0 -- in afara contractului kernelului)

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/worker/stages/agent.py:_persist_safety_context` (L639) | v1 | `assign` | `safety` | Contextul de siguranta (NX-173, P0) e explicit AFARA din contractul kernelului -- propriul lui gate, propriul lui owner, neatins de I3/I20. | `stays` |
| `src/worker/stages/agent.py:_persist_safety_context` (L638) | v1 | `patch_subscript` | `safety` | Contextul de siguranta (NX-173, P0) e explicit AFARA din contractul kernelului -- propriul lui gate, propriul lui owner, neatins de I3/I20. | `stays` |

## `other` -- alte campuri de administrare a starii (pass-through, revizie, schema)

| Scriitor (fișier:funcție) | Cale | Formă AST | Cheie brută | De ce | Soartă |
| --- | --- | --- | --- | --- | --- |
| `src/conversation/state_reducer.py:reduce_all` (L213) | v2 | `dataclasses_replace` | `revision` | Incrementeaza `revision` la finalul unui lot de propuneri -- contabilitatea reducerului insusi, ramane exact aici. | `stays` |
| `src/conversation/state_v2.py:hydrate_state_v2` (L562) | v2 | `dataclasses_replace` | `revision` | Hidratare: `revision` se ia din documentul persistat (nu din coloana DB `state_version`) -- parte din contractul de citire/scriere al reducerului insusi. | `stays` |
| `src/conversation/state_v2.py:serialize` (L834) | v2 | `dataclasses_replace` | `passthrough` | Acelasi `shrink`, jumatatea `passthrough` (coșul e ultimul sacrificat, `safety` niciodata singur) -- mecanism de buget al reducerului. | `stays` |
| `src/worker/processor.py:_build_state_v2` (L371) | v2 | `dataclasses_replace` | `passthrough` | Acelasi `_build_state_v2`: `passthrough` carata verbatim din starea de baza + `state_patch` pentru cheile nemodelate de v2 (`cart` legacy, `safety`) -- mecanismul care garanteaza ca o migrare de format nu rescrie date pe care cardul asta nu le detine (P3). | `stays` |

## `unresolved` -- acces dinamic, nu se poate rezolva static

| Fișier:funcție | Motiv | Linie |
| --- | --- | --- |
| `src/agent/brain.py:_search` | `dynamic_patch_update_arg` | 1159 |
| `src/agent/brain.py:_state_proposals_from_plan` | `dynamic_proposal_op` | 948 |
| `src/agent/tool_executor.py:_execute_serialized` | `dynamic_patch_update_arg` | 324 |

