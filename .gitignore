# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
dist/
build/
*.egg-info/

# Env - NICIODATA in git
.env
.env.local
.env.production
.env.staging
.env.bak*
# NX-248: credentialul de DDL trăiește DOAR aici, pe VPS, montat exclusiv în jobul de migrare.
# Nu are pereche `.example` cu valoare — vezi docs/SECRETS-ROTATION.md.
.env.migrate
# Secrete livrate prin fișier (`<NUME>_FILE=...`), dacă sunt ținute în repo dir pe VPS.
secrets/
*.key
*.pem
*.env.bak
# .env.example se commitează — arată ce variabile sunt necesare

# Node
node_modules/

# VS Code
.vscode/settings.json
.vscode/launch.json

# PyCharm / JetBrains
.idea/

# OS - Windows
Thumbs.db
desktop.ini

# OS - Mac
.DS_Store

# Supabase
supabase/.branches
supabase/.temp

# Pytest
.pytest_cache/
htmlcov/
.coverage

# Logs
*.log
logs/

# Docker
.docker/

# DB dumps — date reale + PII (channel_identities), NICIODATA in git
backups/
*.sql.dump
*.pgdump

# Doc de referință binar (păstrat local, nu în repo)
docs/semantic-cache-architecture-review.md.pdf

# Worktree-uri locale (Claude/Codex) — copii de lucru, niciodată în repo
worktrees/
.codex-worktrees/
.claude/worktrees/

# Artefacte de rulare sim (dump-uri de debug, regenerabile)
scripts/sim/runs/

# NX-247 — harnessul E2E Stage 1.
# Handshake-ul conține secretul de control al procesului și tokenurile publice ale tenanților
# sintetici: efemer, scris la pornire, șters la oprire. Un commit accidental ar publica o cheie
# de control (chiar dacă loopback-only) și ar deveni un tipar de copiat.
.stage1-e2e/
# Certificatul de pereche (SHA backend + SHA frontend) e artefact de CI, nu sursă: îl produce
# rularea, îl consumă NX-249. Un fișier comis ar fi provenance care nu corespunde niciunei rulări.
reports/stage1/
