-r requirements.txt

# Teste
pytest==9.0.3
pytest-asyncio==1.4.0
fakeredis==2.36.1  # 2.28 incompatibil cu redis-py 8.0 (assert pe orice comandă)
pytest-cov==6.2.1

# Linting
ruff==0.11.13

# Retrieval benchmark (NX-203) — implementarea de referință pt Recall/nDCG/MRR peste qrels.
# Harness-ul folosește metrici pure-Python (deterministe, fără dependență la runtime); ir-measures
# e folosit ca CROSS-CHECK la rularea completă (când e instalat) — vezi src/evals/retrieval/.
ir-measures==0.3.6
