# NX-177 — Igienă suită de teste: contracte stale, leftovers, grant, `sim/cleanup.py`

## Context
Audit Codex pe `origin/main`: `ruff check` PASS, `ruff format --check` PASS, audit semantic live
PASS, **pytest pe DB live: 1693 passed / 13 failed**. Eșecurile sunt **mediu + contracte stale**, nu
regresii de produs (vezi `baseline-integration-test-failures` — verifică vs `origin/main` întâi).

Confirmat independent:
- **catalog crescut**: DB live are **654 produse**, testele așteaptă 500 → assert-uri pe număr fix
  (CLAUDE.md „Client demo activ" spune tot 500 → **stale**, vezi `claude-md-data-stale`);
- **`scripts/sim/cleanup.py` LIPSEȘTE** din repo, deși e documentat ca prerechizit în
  [`scripts/sim/server.py:15`](../scripts/sim/server.py#L15) și
  [`scripts/sim/nx172_smoke.py:26`](../scripts/sim/nx172_smoke.py#L26) („Datele `sim:*` sunt
  curățabile cu scripts/sim/cleanup.py"). Există doar ca fișier **untracked** în tree-ul local al
  lui Adi → niciodată commis. Datele `sim:*` se acumulează în DB live la fiecare rulare de smoke.

Raportat de Codex, de re-verificat la implementare:
- date `outbox`/`inbound` rămase între teste (izolare de fixture);
- grant lipsă pe `analytics_events` (rol `bot_runtime`);
- contracte vechi în `test_search_products`.

## Definition of Done
- [ ] Testele nu mai depind de un **număr fix** de produse (assert pe invarianți, nu pe 500/654).
- [ ] Fixture-urile curăță `outbox`/`inbound` (fără leftovers între teste).
- [ ] Grant `analytics_events` pentru `bot_runtime` — verificat pe rolul REAL, nu pe `service_role`.
- [ ] `test_search_products` aliniat la contractul curent.
- [ ] **`scripts/sim/cleanup.py` commis** — purjă idempotentă a datelor `sim:*` (dry-run + `--apply`,
      scoped pe `business_id`, doar `sender_external_id LIKE 'sim:%'`).
- [ ] CLAUDE.md „Client demo activ" actualizat (500 → 654) — `docs-per-task`.
- [ ] `pytest -x -q` verde pe DB live.

## Note
Separă clar **eșec de mediu** (integration lovește Supabase live) de **contract stale** — fixează
contractele, nu masca mediul.
