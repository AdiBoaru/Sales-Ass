# T034 — Rularea 002 pe Supabase + verificare integritate
**Owner:** S · **Faza:** MVP · **Zi:** Z2 · **Branch:** — (operațiune, nu cod; fișierul e deja în PR-uri) · **Complexitate:** S · **Estimare:** 1h

## Goal
Momentul în care 002 devine realitate: rulat cap-coadă pe Supabase, cu dovada că datele demo (221 produse, 284 variante, 38 taxonomii, 11 FAQ) au supraviețuit intacte și că tot ce-au promis T021-T033 există.

## Business Context
DB-ul e singurul element cu stare din tot sistemul — singura componentă unde „am stricat ceva" nu se repară cu redeploy.

## Technical Description
Procedura: (1) snapshot înainte — notează restore point-ul PITR SAU rulează `pg_dump` complet local (T018 are conexiunea directă); (2) count-uri înainte: products, variants, taxonomy, faq, contacts, messages — salvate în fișier; (3) rulează 002 într-O SINGURĂ tranzacție (`psql --single-transaction -f docs/002_schema_fixes.sql`) — orice eroare = rollback total, nu schemă pe jumătate; (4) count-uri după + verificările punctuale din fiecare card (unique-uri, EXPLAIN-uri, RLS fail-closed, funcția GDPR pe sintetic); (5) verifică rolurile + grant-urile (bot_runtime nu poate UPDATE analytics, nu poate executa gdpr_erase).

## Principii CLAUDE.md aplicabile
Toate cele materializate de 002 — asta e validarea lor colectivă.

## Implementation Steps
1. Snapshot + count-uri before.
2. `psql "$DIRECT_URL" --single-transaction -v ON_ERROR_STOP=1 -f docs/002_schema_fixes.sql`
3. Count-uri after + checklist de verificări per task (din cardurile T021-T033).
4. Raport scurt în issue: before/after + bifele. Junior îl contra-verifică.

## Files To Create
- `docs/verif_002_report.md` (raportul before/after)

## Files To Modify
None.

## Database Changes
Aplicarea întregului 002.

## API Changes / Events de emis
None.

## Dependencies
- T021-T033 toate merse în main; T018

## Out of Scope
- 003 (drop deprecated) — rămâne nerulat până după M1.

## Definition of Done
- [ ] 002 rulat fără erori, single-transaction
- [ ] Count-uri identice pe datele demo
- [ ] Toate verificările punctuale bifate în raport
- [ ] Junior a contra-verificat raportul

## Test Cases
**Happy:** totul verde. **Edge:** migrarea durează pe vreun backfill → ON_ERROR_STOP previne jumătățile. **Failure:** eroare la mijloc → rollback automat; fix în fișier; re-run de la zero (fișierul e idempotent unde se poate: IF NOT EXISTS).
