# T020 — Review 001_schema_v1 vs CLAUDE.md
**Owner:** S · **Faza:** MVP · **Zi:** Z1 · **Branch:** — (analiză, output = listă) · **Complexitate:** XS · **Estimare:** 1h

## Goal
Inventarul exact al diferențelor dintre schema care RULEAZĂ acum în Supabase (001 + 010) și schema promisă de CLAUDE.md actualizat. Output-ul este lista de verificare pentru T021-T033 — fără ea, migrarea 002 se scrie din memorie și uită lucruri.

## Business Context
002 e ultima șansă de a corecta schema ieftin: după ce pipeline-ul scrie în ea, orice schimbare structurală costă triplu.

## Technical Description
Compari, tabel cu tabel: (1) tabelele noi cerute de CLAUDE.md [002]: outbox, channel_identities, checkout_links, product_embeddings, review_summaries, wa_templates, usage_daily, gdpr_requests, audit_log, back_in_stock_subscriptions; (2) coloanele noi pe tabele existente: contacts(last_inbound_at, consent, erased_at, display_name?), messages(provider_message_id, status), orders(attributed_checkout_link_id, attribution), faq/response_cache/clarification_templates(language); (3) rolurile DB există? (bot_runtime, catalog_sync, dashboard_read) — în 001 sau de creat în 002; (4) extensia `vector` activă? (`select * from pg_extension`); (5) ce date din 010 ating migrarea (backfill-uri necesare). Output: checklist în issue-ul T020, cu bifă per item → devine ordinea de scriere a migrării.

## Principii CLAUDE.md aplicabile
P7 — verifici că TOATE tabelele noi au business_id în design; P12 — confirmi unde trăiește PII azi.

## Implementation Steps
1. Export schema curentă: `pg_dump --schema-only` din Supabase (conexiunea directă 5432).
2. Diff manual contra secțiunii Schema DB din CLAUDE.md, construind checklist-ul.
3. Verifică extensii + roluri existente cu query-uri de catalog.
4. Postează checklist-ul în issue; el devine cuprinsul fișierului 002.

## Files To Create / Files To Modify
None (output = issue/checklist).

## Database Changes
None — doar citire.

## API Changes / Events de emis
None.

## Dependencies
- T012 (CLAUDE.md final în repo), acces Supabase (T018)

## Out of Scope
- Scrierea SQL-ului (T021-T033). Orice „hai că o repar acum" e scope creep.

## Definition of Done
- [ ] Checklist complet cu fiecare tabel/coloană/index/rol marcat „există / lipsește / diferă"
- [ ] Extensia vector + rolurile verificate explicit
- [ ] Validat de junior (a doua pereche de ochi pe inventar)

## Test Cases
**Happy:** checklist-ul acoperă toate intrările [002] din CLAUDE.md. **Edge:** coloană existentă dar cu alt tip decât cere CLAUDE.md → marcată „diferă" cu plan de ALTER. **Failure:** pg_dump pică pe pooler 6543 → folosește conexiunea directă 5432 (documentat în T018).
