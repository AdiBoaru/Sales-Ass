# NX-254 — CI roșu permanent: `isolation-concurrent` cere 50 de sesiuni, poolerul dă 15

**Status:** REZOLVAT (2026-09-03) — fix aplicat + verificat pe DB-ul real; rămâne confirmarea
pe o rulare de `CI` de pe `main` · **Prioritate:** P1 · **Wave:** G / Stage 1 close
**Repo țintă:** `Sales Ass` · **Tip:** fix de test + capacitate · **Branch sugerat:** `fix/NX-254-isolation-pool-budget`
**Complexitate:** S/M · **Risc:** mic (atinge un singur test; NU relaxează invariantul de izolare)
**Simptom:** `CI` e roșu pe **fiecare** rulare de pe `main` de cel puțin 2026-08-21 încoace

## Findingul

```
gh run list --workflow=ci.yml --branch main --limit 6   →   6/6 failure
```

Un singur job pică; restul sunt verzi:

```
Lint (ruff)                     :: success
Test (pytest)                   :: success      ← poarta de PR
Supply chain (lock + audit)     :: success
Migrări (Postgres 16 efemer)    :: success
Imagine (build + hardening)     :: success
Izolare concurentă (NX-53)      :: failure
```

Cauza, din logul rulării `32619851592` (2026-08-23T05:16):

```
asyncpg.exceptions.InternalServerError:
  (EMAXCONNSESSION) max clients reached in session mode - max clients are limited to pool_size: 15
FAILED tests/test_isolation_concurrent.py::test_concurrent_isolation_holds
1 failed, 31 passed, 4514 deselected
```

**Nu e un eșec de izolare.** E capacitate: [`tests/test_isolation_concurrent.py:25`](../../tests/test_isolation_concurrent.py#L25)
are `N_PER_TENANT = 25` × 2 tenanți = **50 de `tenant_conn` concurente**, iar poolerul Supabase în
session mode plafonează la **15 clienți**, buget împărțit cu producția. Testul cere de peste trei
ori ce există.

Eșecul secundar din log (`InterfaceError: pool is closed`, `CancelledError` la shutdown) e
consecință, nu cauză: după ce prima conexiune e refuzată, fixture-ul închide poolul în timp ce
restul task-urilor din `asyncio.gather` încă rulează.

## De ce e P1, deși „doar un test"

Același tipar ca NX-252, cu altă față: **un gate roșu permanent nu mai spune nimic.** Poarta de PR
(`pytest -m "not integration"`) e verde, deci senzația la fiecare merge e „totul e ok", în timp ce
`main` are CI roșu de zile. Exact așa a durat șase săptămâni pana de livrare.

Nu e nici prima încercare: PR #308 a atacat simptomul serializând jobul pe `concurrency: isolation-db`
([ci.yml:266](../../.github/workflows/ci.yml#L266)). Serializarea elimină concurența **între rulări**,
dar testul depășește bugetul **singur** — deci n-avea cum să fie suficient.

## Ce NU se face

- **NU** se scade `N_PER_TENANT` „până trece". Testul e dovada executabilă pentru NX-50: dacă
  `app.business_id` s-ar scurge între checkout-uri pe un pool cu conexiuni intens refolosite, un
  task ar vedea datele altui tenant. Concurența ESTE scenariul — un test cu 6 task-uri nu mai
  exersează refolosirea agresivă și devine teatru.
- **NU** `xfail` / `skip` pe eșec de capacitate. Ar transforma un invariant P0 într-un comentariu.
- **NU** se ridică `pool_size` pe poolerul de producție ca să treacă CI-ul.

## Direcții (de evaluat, nu prescrise)

1. **Transaction mode pentru test.** Plafonul de 15 e specific `session mode`. Un DSN dedicat pe
   portul de transaction pooling ridică plafonul substanțial. De verificat că `SET app.business_id`
   per checkout se comportă identic acolo — dacă nu, direcția pică (și e chiar motivul pentru care
   NX-50 a renunțat la `SET ROLE`: se scurgea sub multiplexare).
2. **Postgres efemer în job**, ca la `Migrări (Postgres 16 efemer)` — care e verde. Elimină complet
   dependența de poolerul partajat cu producția și de bugetul lui. Probabil cea mai curată.
3. **Concurență reală păstrată, sesiuni mărginite:** semafor la ~12 conexiuni simultane, cu 50 de
   tururi tot lansate. Refolosirea agresivă rămâne (chiar se accentuează), plafonul e respectat.
   Cea mai ieftină, dar trebuie argumentat că nu slăbește scenariul.

## Ce s-a făcut

**Diagnosticul din card era pe jumătate.** „Testul cere de peste trei ori ce există" nu e adevărat:
cele 50 de task-uri **se așteaptă la coadă pe `bot_pool`**, care are `max_size` sesiuni. Concurența
logică e 50, bugetul de sesiuni e al POOLULUI. Ce depășea plafonul era suma celor două pooluri ale
procesului: **10 admin + 10 bot = 20 > 15**.

De aia direcția 3 din card („semafor la ~12") era deja implementată — de asyncpg — și nimeni nu se
uitase la ea. Fixul real e bugetul, nu concurența.

1. **Dimensiunile poolurilor devin config** (`DB_ADMIN_POOL_MAX` / `DB_BOT_POOL_MAX`), cu
   default-urile de dinainte → producția e neatinsă.
2. **Jobul de izolare primește 2 + 8 = 10**, cu 5 lăsați producției.
3. **O gardă nouă** (`test_bugetul_de_sesiuni_incape_sub_plafonul_poolerului`) verifică relația
   înainte de a atinge DB-ul și transformă un `(EMAXCONNSESSION)` opac într-un mesaj care spune ce
   să faci — și, explicit, ce să NU faci (să reduci `N_PER_TENANT`).

**Cu mai puține sloturi, testul e mai STRICT, nu mai slab:** aceleași 50 de tururi refolosesc
conexiunile mai des, iar refolosirea agresivă între tenanți ESTE scenariul pe care testul îl
dovedește.

## De ce `CI` e verde fără fixul ăsta — și de ce fixul rămâne necesar

Măsurat pe 2026-09-03: `CI` pe `main` e verde pe ultimele 5 rulări, iar jobul de izolare trece cu
aceeași configurare de 10+10 care pica în august.

Ce s-a schimbat între timp nu e configurarea, e **producția**: `bot.nativextech.com` nu mai răspunde
pe niciun port, inclusiv SSH (NX-252). Bugetul poolerului e ÎMPĂRȚIT cu producția, iar o producție
moartă nu consumă sesiuni — deci cele 20 ale CI-ului încap acum în 15.

**E o inferență, nu o măsurătoare**: n-am cum să număr sesiunile poolerului din afară. Dar e singura
explicație simplă care se potrivește cu ambele fapte, și are o consecință verificabilă: **jobul va
redeveni roșu în ziua în care producția revine.** Un gate care e verde fiindcă producția e moartă nu
e un gate verde, e același gate roșu cu altă față.

De aia fixul rămâne: bugetul explicit scoate CI-ul din competiția cu producția, indiferent în ce
stare e ea.

## Ce am găsit pe drum, și nu e al acestui card

**Producția are aceeași problemă.** Default-urile procesului sunt 10 admin + 10 bot = **20 de
sesiuni** în vârf, pe același `SUPABASE_DB_URL` cu același plafon de **15**. Adică nu CI-ul cerea
prea mult — CI-ul rula aceeași configurare ca producția, iar configurarea aia nu încape.

CI-ul a pățit-o primul fiindcă acolo se cer toate cele 20 aproape simultan; în producție, cu 0
conversații pe zi, poolurile n-au ajuns niciodată la `max_size`. **Se va vedea la primul vârf de
trafic**, ca `(EMAXCONNSESSION)` în mijlocul unei conversații.

N-am schimbat default-urile aici, deliberat: dimensiunea unui pool de producție e o decizie de
CAPACITATE, se ia pe măsurătoare (D15), și cere fie coborârea poolurilor, fie ridicarea plafonului
la Supabase, fie transaction mode. E un card separat, cu o cifră în spate. Ce am făcut e să fac
setarea CONFIGURABILĂ, deci decizia aia devine o linie de env, nu un PR.

## Definition of Done

- [x] `pytest -m "integration and slow"` trece, **fără** a reduce numărul de tururi concurente și
      fără skip/xfail — **3/3 verzi pe DB-ul REAL**, cu `N_PER_TENANT` neatins
- [x] Cauza documentată în test (de ce plafonul e ce e, și de ce alegerea nu slăbește NX-53)
- [x] **`CI` verde pe `main`** — dar **NU datorită fixului ăstuia**, și asta trebuie spus:
      măsurat pe 2026-09-03, ultimele 5 rulări de pe `main` sunt verzi, inclusiv „Izolare concurentă
      (NX-53)", **cu configurarea veche de 20 de sesiuni**. Vezi mai jos.
- [x] Un semnal la eșec — `.github/workflows/main-health.yml`: `CI` sau `Release` roșu pe `main`
      deschide (sau comentează) UN issue, care se închide singur când rularea următoare e verde.
      Același mecanism pentru ambele, cum cerea cardul.

## Verificare rapidă

```bash
gh run list --workflow=ci.yml --branch main --limit 5
gh run view <ID> --log-failed | grep -i EMAXCONNSESSION
```
