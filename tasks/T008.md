# T008 — Schelet foldere src/
**Owner:** J · **Faza:** MVP · **Zi:** Z1 · **Branch:** `chore/T008-skeleton` · **Complexitate:** XS · **Estimare:** 1h

## Goal
Materializează structura din CLAUDE.md în foldere reale, ca fiecare task următor să știe exact unde scrie și ca importurile să funcționeze din ziua 1.

## Business Context
Structura = harta. Claude Code lucrează mult mai precis când calea din card există deja.

## Technical Description
Creează exact arborele din CLAUDE.md: `src/{webhook,worker,worker/stages,tools,agent,db,db/queries,proactive,gdpr,jobs}` + `tests/golden` + `prompts/` + `docs/` + `scripts/`, fiecare pachet Python cu `__init__.py` gol. `src/__init__.py` include `__version__`. Adaugă `tests/test_imports.py` care importă toate pachetele (smoke test că structura e validă).

## Principii CLAUDE.md aplicabile
Structura proiectului din CLAUDE.md e contractul — zero abateri fără PR pe CLAUDE.md.

## Implementation Steps
1. `mkdir -p` pe tot arborele + `touch __init__.py`-uri.
2. `tests/test_imports.py` cu importurile.
3. PR + merge.

## Files To Create
- Tot arborele de `__init__.py`
- `tests/test_imports.py`

## Files To Modify
None.

## Database Changes / API Changes / Events de emis
None.

## Dependencies
- T006

## Out of Scope
- Orice fișier cu logică (vin în taskurile lor).

## Definition of Done
- [ ] `python -c "import src.worker.stages, src.tools, src.db.queries"` merge
- [ ] Structura identică cu CLAUDE.md (diff manual)
- [ ] test_imports verde în CI

## Test Cases
**Happy:** toate importurile trec. **Edge:** import circular imposibil acum (pachete goale) — testul devine canarul pentru mai târziu. **Failure:** folder fără __init__.py → ImportError prins de test.
