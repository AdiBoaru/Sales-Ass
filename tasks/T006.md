# T006 — requirements.txt pin-uit
**Owner:** J · **Faza:** MVP · **Zi:** Z1 · **Branch:** `chore/T006-requirements` · **Complexitate:** XS · **Estimare:** 1h

## Goal
Dependențe fixe = build-uri reproductibile. „Merge la mine" dispare ca categorie de probleme.

## Business Context
Deploy-ul pe VPS și CI-ul trebuie să instaleze EXACT ce ai testat local.

## Technical Description
`requirements.txt` cu versiuni exacte (==): fastapi, uvicorn[standard], asyncpg, redis, openai, pydantic (v2), pydantic-settings, httpx, structlog. `requirements-dev.txt`: pytest, pytest-asyncio, fakeredis, ruff, pytest-cov. Versiunile: cele mai recente stabile la data instalării (rulezi `pip install X && pip freeze | grep -i X`). Politica de upgrade: manual, într-un PR dedicat, nu „latest" automat.

## Principii CLAUDE.md aplicabile
P4 în spirit: constrângerile (aici versiunile) trăiesc în fișiere, nu în memoria oamenilor.

## Implementation Steps
1. venv curat: `python3.12 -m venv .venv && source .venv/bin/activate`.
2. Instalează pachetele, rulează `pip freeze` și extrage liniile relevante (NU tot freeze-ul — doar dependențele directe).
3. Test pe venv nou: `pip install -r requirements.txt -r requirements-dev.txt`.
4. PR + merge.

## Files To Create
- `requirements.txt`
- `requirements-dev.txt`

## Files To Modify
None.

## Database Changes / API Changes / Events de emis
None.

## Dependencies
- T001

## Out of Scope
- Lock file complet (poetry/uv) — decizie amânată P2; requirements pin-uit ajunge acum.

## Definition of Done
- [ ] Instalare curată pe Python 3.12 fără erori/conflicte
- [ ] Toate versiunile cu `==`
- [ ] CI folosește ambele fișiere

## Test Cases
**Happy:** venv nou + install + `python -c "import fastapi, asyncpg, redis, openai"`. **Edge:** instalare pe VPS (alt OS) funcționează identic. **Failure:** conflict de versiuni → pip explodează la install, nu la runtime.
