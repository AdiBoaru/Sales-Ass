# T011 — Makefile / scripts dev
**Owner:** J · **Faza:** P1 (O2) · **Branch:** `chore/T011-makefile` · **Complexitate:** XS · **Estimare:** 0.5h

## Goal
Comenzile zilnice memorabile: `make run`, `make test`, `make lint`. Mutat în P1 la rebalansare — util, dar nu blochează nimic.

## Business Context
Reduce frecarea zilnică; nu produce valoare direct.

## Technical Description
Makefile cu țintele: `run` (docker compose up), `stop`, `test` (pytest -x -q), `test-int` (pytest -m integration), `lint` (ruff check + format --check), `fmt` (ruff format), `logs` (compose logs -f).

## Implementation Steps
1. Scrie Makefile. 2. Testează fiecare țintă. 3. Menționează în README. 4. PR.

## Files To Create
- `Makefile`

## Files To Modify
- `README.md` (secțiunea comenzi)

## Database Changes / API Changes / Events de emis
None.

## Dependencies
- T009

## Out of Scope
- Comenzi de deploy (T159-T161 au ale lor).

## Definition of Done
- [ ] Toate țintele funcționează pe mașinile ambilor

## Test Cases
**Happy:** make test = pytest. **Edge:** make pe Windows? — junior pe Windows folosește WSL (documentat). **Failure:** țintă inexistentă → make dă eroare standard.
