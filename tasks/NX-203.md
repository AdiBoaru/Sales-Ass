# NX-203 — Retrieval benchmark set (≥100 FAMILII distincte) + harness ir-measures

**Owner:** Claude (build) / Codex (verify) · **Faza:** 1 (Quality Overhaul) · **Status:** DRAFT rev.2 (post-review Codex) · **Complexitate:** M
**Parent plan:** [docs/QUALITY-OVERHAUL-2026.md](../docs/QUALITY-OVERHAUL-2026.md) · **Depinde de:** — (paralel cu NX-202)
**Decizii ADR aplicabile:** D3, D15

> ## Contract schimbat 2026-07-31 — ținta e în FAMILII, nu în query-uri
> **Ținta headline: ≥100 familii distincte, integral human-verified** (`TARGET_FAMILIES`).
> Titlul vechi („200-500 query-uri") măsura altceva decât metrica: `run_benchmark` face media ÎN
> familie, apoi macro peste familii, deci rezoluția crește cu numărul de contracte de adevăr
> distincte, nu cu numărul de formulări. Ținta veche se putea atinge adăugând diacritice și typo-uri.
> `n_queries` rămâne raportat ca indicator de **robustețe la formă**; nu e gate de calitate. Dacă
> „200" se păstrează, e indicator secundar de variație.
> **Deficitul se acoperă cu intenții NOI, nu cu parafraze.**

> ## 🔴 CORPUSUL VECHI E EXPIRAT — repornit pe SOLE 2026-09-09
> Nu era pauzat, era mort. `tests/golden/qrels_confirmed.json` are `business_id` =
> `6098812a-…` (tenantul demo, eu-west-1), `catalog_version = demo-2026-07-22` pe toate cele 18
> intrări, iar `judgments` referă UUID-uri de produs dintr-un proiect Supabase **abandonat din
> 2026-08-28**. Toate cele 6 scripturi `nx203_*.py` de atunci aveau acel `business_id` hardcodat.
> Regula e chiar în §2 de mai jos: *etichetele expiră la schimbarea catalogului*. Aici s-a
> înlocuit baza de date întreagă (150 de produse beauty demo → 2.758 de produse SOLE).
>
> **Consecințe:** cele 18 familii confirmate și cele 61 de query-uri din loturile 4/5a sunt
> nefolosibile; cele trei decizii care așteptau owner-ul (fuziunile invalide de la verdictele 10 și
> 11, amendamentul `q-con-06`/`vitamina c`) sunt fără obiect — privesc produse care nu mai există.
> Aritmetica „64 familii → +36" nu se aplică. Punctul de plecare real e **0 familii pe SOLE**.
>
> **Ce a supraviețuit:** metodologia (familia ca unitate, relevanță graduală, `forbidden` explicit,
> split H1/H2/H3 single-use) și validatoarele — sunt agnostice de catalog. Partea scumpă de design
> nu s-a pierdut.
>
> **Ce s-a construit în loc (2026-09-09), pe tenantul `sole-ro`:**
> 1. `scripts/nx203_extract_families.py` — familii candidate din cele **12.665 de fraze reale de
>    căutare** din `product_sections.kind='recommendation_trigger'` (~5 per produs, scrise ca de
>    client). Rezultat: **779 familii · 38 tipuri de produs**, `catalog_version=sole-f4b26a0def37`
>    (amprentă SHA-256 peste ID-urile de produs: se schimbă când apare sau dispare marfă, nu când
>    se schimbă un preț). Cheia de familie = tip de produs × fațete rezolvate × termeni nerezolvați.
> 2. `scripts/nx203_build_pools.py` — pool de candidați cu **două brațe**: retrievalul real (text
>    brut + text cu constrângeri, pe conexiune `bot_runtime` cu RLS activ) ȘI produsele afirmate de
>    comerciant. Al doilea braț nu trece prin motorul nostru, deci acoperă exact punctul lui orb.
>    Măsurat pe 120 de familii stratificate pe tip: **1.920 de candidați, dintre care 157 (8%) apar
>    DOAR pe brațul comerciantului** — un pool cu un singur braț i-ar fi dat motorului recall
>    perfect prin construcție.
> 3. `scripts/nx203_label.py` — etichetare la tastatură, autosave la fiecare apăsare, resume,
>    `undo`, și spargerea unei familii. Ordinea candidaților e o permutare deterministă derivată din
>    `family_id`, NU rangul motorului: altfel judecata umană s-ar ancora pe motor.
>
> **Limita găsită, care nu se repară prin cod:** familiile rămân prea largi acolo unde catalogul e
> sărac în fațete (părul n-are `concerns`, ingredientele rare nu-s în `attributes`), deci fuzionează
> cereri diferite — „cremă de față cu Guaiazulene" ajunge lângă „cremă de față cu peptide".
> **Un benchmark nu poate fi mai fin decât vocabularul care descrie adevărul.** Rezolvarea aleasă:
> omul sparge familia din unealtă, iar lista spargerilor devine inventarul de fațete lipsă (input
> pentru NX-268/271). Alternativa — întâi închidem golurile de vocabular — ar face NX-203 dependent
> de alt proiect.
>
> 4. `scripts/nx203_finalize.py` — din judecăți în `QrelsSet`, apoi validare + felii. NU
>    re-implementează nimic: consumă harnessul existent (`src/evals/retrieval/`). Trei reguli de
>    includere, toate din „într-un instrument de măsură, degradarea grațioasă e o minciună":
>    familia închisă cu `s`/`k` nu intră (contract inexistent); familia etichetată PARȚIAL nu intră
>    (numitorul recall-ului ar fi arbitrar); familia fără produse relevante merge la ABSTENȚIE —
>    alt contract, alt fișier.
>
> **Lanțul complet, verificat cap-coadă pe ieșirea reală a uneltei:**
>
> ```
> python scripts/nx203_extract_families.py --business <uuid> --write
> python scripts/nx203_build_pools.py --business <uuid> --limit 150 --write
> python scripts/nx203_label.py                    # pasul UMAN: 2.332 judecăți / 150 familii
> python scripts/nx203_finalize.py --business <uuid> --write
> ```
>
> Ținta de 100 de familii se atinge după ~1.500 de judecăți; restul e rezervă pentru familiile
> închise ca fuziuni. Corpusul vechi rămâne în repo ca istoric, marcat expirat.
>
> **Defecte găsite la verificarea adversarială (2026-09-09), toate reparate:** negația nu intra în
> contractul de adevăr („cu volum" și „fara volum" erau aceeași familie); fixul pentru ea era INERT
> (prefixul `!` se compara cu vocabularul catalogului, deci pica mereu); detectorul de fuziuni
> măsura cuvintele comune prin construcție (3 marcate din 779 → 40); brațul comerciantului putea
> elimina complet motorul din pool; iar în infrastructura EXISTENTĂ, `constraints.py` nu cunoștea
> `product_type`, deși e axa principală a fiecărei familii — orice constrângere pe ea ieșea
> `unknown`, adică „n-am verificat", indistinct în raport de „zero violări". 36 de teste pe
> funcțiile pure, fiecare numind defectul real care l-a cerut.
>
> **Rămas deschis, măsurat:** construcția „rezistent la X" inversează sensul
> („machiaj rezistent la apa" → `key_ingredients=apa`). 2 familii din 150. `key_ingredients` e
> listă TOLERANTĂ în evaluator, deci constrângerea greșită nu produce violări false — dar rămâne
> de corectat la validare, înainte de sigilare.

> ## ⏸ PAUZAT 2026-07-31 — unde s-a oprit (ISTORIC: privește catalogul demo, expirat)
> Infrastructura de măsurare e **terminată și verde** (harness + catalog ca input + gărzi).
> Corpusul e **oprit înainte de etichetare**, cu tot ce s-a generat comis.
>
> **De reluat, în ordine:**
> 1. Aprobă/corectează drafturile `_lot4_draft.json` (19q) și `_lot5a_draft.json` (13q) —
>    `human_verified=false` peste tot; nimic nu urcă steagul automat.
> 2. Decide pe cele **două fuziuni invalide** găsite de matricea de egalitate: verdict 10
>    (`q-con-03` n-are `suitable_for=dry`, deși textul zice „le am cam uscate") și verdict 11
>    (`q-con-01` are `texture in [cremă,fluid]`, `lot6-05` nu). Ambele cer ori amendarea unei
>    intrări `human_verified`, ori anularea fuziunii.
> 3. `_q-con-06_amendment_draft.json`: constrângerea `key_ingredients='vitamina c'` e **inertă**
>    (catalogul scrie `'vitamina C'`, zero produse o satisfac). Cele două seruri din auditul de
>    recall sunt marginale (1), nu relevante — nu conțin vitamina C.
> 4. Draftează lot5b (7 cross-categorie, 228 perechi), lot6 (14), lot7 (7 — blocul de
>    disponibilitate se decide ca bloc: intră în corpus sau într-o suită separată).
> 5. Rulează `scripts/nx203_reconcile_manifest.py --apply` **după fiecare** lot scris în qrels
>    (impus de `tests/test_qrels_manifest_reconciled.py`).
>
> **Aritmetica de reluat:** 18 confirmate + 61 în loturi = 79 query-uri ≈ **64 familii**. Ținta e
> 100 → deficitul de ~36 se acoperă cu **intenții noi** (trafic real nou sau intenții scrise de la
> zero, ancorate în catalog), nu cu parafraze.
>
> **Suite separate, nu în cele 100:** rutine (6), cadouri (5), comparații (3), abstenție (20 —
> include `lot4-13`, `lot6-06`, `lot6-15`, „calm theory glow").
>
> Blocante neatinse, cum au fost cerute: `search_entities_shadow` neînregistrat,
> `SEARCH_SHADOW_ENABLED=false`, migrarea 036 neaplicată, holdout H2 nedeschis.

## Goal
Metrul de măsură pentru retrieval: setul pe care se aleg documentele, embeddings-urile și
rerankerul — SEPARAT de golden set-ul conversațional (alt volum, alte metrici).

## Scope
1. **200-500 query-uri ro-RO** cu qrels. Acoperire: exact match (nume/brand/SKU) · colocvial
   (concern_map) · compuse (multi-constraint) · typo/diacritice lipsă · negative („fără parfum",
   „nu brandul X").
2. **Metodologia qrels (obligatorie):**
   - **relevanță GRADUALĂ** (ex. 0-3), nu binar relevant/irelevant;
   - **hard constraints așteptate** per query;
   - **produse INTERZISE explicit** (nu doar „nerelevante");
   - **proveniența per query:** `real_sanitized` | `synthetic` | `paraphrase`, cu **minim de
     query-uri reale sanitizate per categorie**;
   - **`catalog_version`** pe care s-au făcut etichetele (etichetele expiră la schimbarea catalogului);
   - **verificare umană pentru TOATE variațiile generate** (nimic sintetic neverificat);
   - **test de integritate qrels** (produse inexistente, contradicții relevant/interzis, duplicate).
3. **Split OBLIGATORIU tuning vs holdout, cu felii SINGLE-USE per gate (versionat):** setul se
   împarte de la început în **dev/tuning** (pe el se aleg documentele, embeddings-urile,
   rerankerul, ponderile) și **holdout de retrieval** partiționat în FELII dedicate, stratificate
   pe categoriile de query: **H1** (gate-ul de switch NX-207), **H2** (gate-ul NX-209),
   **H3** (evaluarea NX-210). **O felie se deschide O SINGURĂ DATĂ, la gate-ul ei; după
   deschidere e „arsă"** — se retrogradează în pool-ul de tuning și NU mai validează nimic
   (reutilizarea aceluiași holdout la gate-uri succesive = contaminare: deciziile dintre
   gate-uri ar fi informate de el). Feliile se realimentează din query-urile noi etichetate de
   ritualul săptămânal (NX-215). Split-ul + istoricul deschiderilor sunt versionate în repo;
   mutarea unui query între seturi = schimbare cu review.
4. **Harness `ir-measures`** (dev-only): Recall@20, nDCG@6, Top-6 hit rate, încălcări de
   constrângeri, latență, cost per query. **Pin în `requirements-dev.txt`** (nu există azi).
   Output **machine-readable** și comparabil între rulări, cu **configurația completă a rulării**
   înregistrată (model embeddings, `document_version`, ponderi/weights, reranker, data).
   **Intervale de încredere** raportate (nu doar medii punctuale).
5. **Baseline-ul configurației ACTUALE** (lexical + semantic + fusion existente din
   `src/tools/catalog_tools.py`) publicat — pe AMBELE splits (o singură dată; ulterior
   holdout-ul doar la gate-uri).

## Out of Scope
Schimbări de retrieval (NX-207/NX-209). Aici doar setul + harness-ul + baseline.

## Definition of Done
- [ ] ≥200 query-uri cu qrels graduale, versionate în repo, cu proveniență + `catalog_version`.
- [ ] Split tuning/holdout versionat, stratificat, cu felii H1/H2/H3 dedicate per gate;
      holdout exclus din orice buclă de alegere; istoricul deschiderilor înregistrat
      (o felie deschisă = arsă, retrogradată în tuning; verificabil în istoric).
- [ ] Test de integritate qrels verde; toate variațiile generate verificate uman.
- [ ] `ir-measures` pinned în `requirements-dev.txt`; harness rulabil cu o comandă.
- [ ] Output machine-readable cu config-ul rulării + intervale de încredere.
- [ ] Baseline publicat: Recall@20 / nDCG@6 / Top-6 / latență / cost pe configurația actuală.
- [ ] Zero PII în query-urile sanitizate (test dedicat); toate rulările scoped pe `business_id`.
