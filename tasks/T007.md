# T007 — .env.example complet
**Owner:** J · **Faza:** MVP · **Zi:** Z1 · **Branch:** `chore/T007-env-example` · **Complexitate:** XS · **Estimare:** 0.5h

## Goal
Contractul de configurare al aplicației: orice variabilă citită de cod există aici cu nume + comentariu, fără valori reale.

## Business Context
Onboarding junior + deploy VPS + debugging „de ce nu merge" — toate pornesc de la acest fișier.

## Technical Description
Secțiuni comentate: **Supabase** (SUPABASE_DB_URL — connection string Postgres direct, port 5432/6543 pooler), **OpenAI** (OPENAI_API_KEY, MODEL_AGENT=gpt-5.4-mini, MODEL_TRIAGE=gpt-5.4-nano, MODEL_EMBED=text-embedding-3-small), **Meta** (META_ACCESS_TOKEN, META_APP_SECRET, META_VERIFY_TOKEN, META_PHONE_NUMBER_ID), **Redis** (REDIS_URL=redis://redis:6379/0), **App** (ENV=dev|staging|prod, LOG_LEVEL, DAILY_COST_CAP_USD=5, OPERATOR_ALERT_WEBHOOK). Notă: botul folosește SUPABASE_DB_URL (asyncpg direct), NU clientul HTTP supabase — conexiune nativă, tranzacții reale.

## Principii CLAUDE.md aplicabile
Regula 9 CONTRIBUTING: numele variabilelor în repo, valorile niciodată.

## Implementation Steps
1. Scrie fișierul cu toate variabilele + comentariu pe fiecare.
2. Sincronizează cu T040 (config.py citește exact aceste nume) — dacă T040 adaugă variabile, se actualizează ȘI aici (regulă permanentă).
3. PR + merge.

## Files To Create
- `.env.example`

## Files To Modify
None.

## Database Changes / API Changes / Events de emis
None.

## Dependencies
- T006

## Out of Scope
- Valorile reale (le pui local în `.env` și pe VPS în T158).

## Definition of Done
- [ ] `cp .env.example .env` + completare = aplicația bootează (verificabil după T040)
- [ ] Zero valori reale în fișier
- [ ] Fiecare variabilă are comentariu cu rolul ei

## Test Cases
**Happy:** diff între variabilele din config.py și .env.example = gol. **Edge:** variabilă opțională marcată explicit `# optional`. **Failure:** secret real comis → gitleaks (T158) l-ar prinde; până atunci, review.
