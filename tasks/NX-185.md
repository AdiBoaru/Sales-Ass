# NX-185 — QuerySpec shadow: contract + merger owner-unic

**Owner:** Claude (build) / Codex (verify) · **Faza:** P2 · **Track:** Selection Correctness (epic separat) · **Branch:** `feat/NX-185-queryspec-shadow` · **Complexitate:** M · **Estimare:** 3-4 zile
**Parent plan:** [docs/RESPONSE-QUALITY-EPIC.md](../docs/RESPONSE-QUALITY-EPIC.md) · **Depinde de:** — (poate rula în paralel cu Track 0-2; NU blochează naturalețea)

## Goal
Introdu `QuerySpec` — contractul tipizat, canonic al constrângerilor — în **shadow mode**: emis de triaj, combinat de un **merger PUR** cu state-ul, cu **owner unic**. ZERO schimbare de comportament în v1 (doar telemetrie + comparație cu selecția curentă).

## Business Context
Azi „ce căutăm" are 3 reprezentări concurente: `RouteDecision.filters` (triage.py:279), `state.search_constraints` (merge în agent.py:322-324) și **argumentele `search_products` alese de tool-loop**. Modelul poate slăbi o constrângere hard prin args proprii. QuerySpec devine sursa unică; args-urile tool-ului devin o **proiecție**, nu a doua sursă de adevăr.

## Contract
```json
{ "version": 1, "intent": "recommend", "subject": {"category": "creme"},
  "constraints": [
    {"facet": "price", "op": "lte", "value": 80, "strength": "hard", "source": "current_turn"},
    {"facet": "fragrance_free", "op": "eq", "value": true, "strength": "hard", "source": "current_turn"},
    {"facet": "texture", "op": "eq", "value": "light", "strength": "soft", "source": "current_turn"} ],
  "sort": "relevance", "reference_set": [] }
```

## Ownership (rezolvarea dublului owner)
- **Extracție:** triaj (deja deține `RouteDecision.filters`).
- **Merge/persist:** modul PUR NOU (`src/agent/query_spec.py`), NU agent.py (unde e împletit cu tool-loop-ul).
- Constrângerea explicită din turul curent CÂȘTIGĂ; topic-switch resetează moștenirea; revocarea explicită elimină; tool-loop-ul poate formula query-ul semantic dar NU slăbește hard constraints.
- Hard/soft: buget/negație/brand/produs/variantă/stoc/restricție/safety = hard; „aș prefera"/ranking/rating = soft; ambiguu → confidence + planner cere clarificare când schimbă material rezultatul. **SafetyPolicy rămâne poartă separată, neslăbibilă de QuerySpec.**

## Câmpuri TurnContext scrise
`ctx.query_spec` (owner: mergerul pur). Serializat fără PII / fără text brut.

## Implementation Steps
1. Schema + Pydantic; extracție hard/soft în triaj.
2. Merger pur (turn curent + state + revocare + topic-switch).
3. `SearchArgs` = proiecție a QuerySpec (shadow: comparat, nu aplicat).
4. Telemetrie `query_spec_shadow` + `query_spec_disagreement` (fără PII).

## Files To Create / Files To Modify
- **Create:** `src/agent/query_spec.py`, `src/db/migrations/0NN_*` (dacă persistăm ceva normalizat).
- **Modify:** `src/worker/stages/triage.py`, `src/models.py`, `src/config.py`.

## Database Changes
Persistență normalizată (fără PII) doar dacă e necesară — altfel per-tur.

## Rollout & Kill-switch
- **Kill-switch:** `query_spec_shadow_enabled` (default OFF).
- **Rollout:** shadow global → comparație cu selecția veche, **fără** a schimba răspunsul.

## Dependencies
None hard. Precede NX-186/187.

## Out of Scope
- Aplicarea QuerySpec (Match Gate = NX-187/188).
- Typed SQL (NX-189).

## Definition of Done
- [ ] QuerySpec produs pe fiecare tur sales; owner unic (merger pur, nu agent.py).
- [ ] `query_spec_disagreement` emis când tool-loop-ul ar slăbi un hard constraint — **DOAR DETECȚIE** (shadow nu previne).
- [ ] ZERO schimbare de comportament de răspuns (shadow).
- [ ] Zero PII în QuerySpec persistat/telemetrie.
- [ ] `ruff` + `pytest -x -q` + `test_tenant_isolation` verzi.

## Shadow vs Enforce (delimitare de contract)
- **Shadow ≠ enforce (separat explicit):** în SHADOW, QuerySpec rămâne nemodificat, emite `disagreement`, iar răspunsul VECHI continuă — nu previne nimic. **Prevenirea** (SearchArgs = proiecția OBLIGATORIE, args care slăbesc hard constraints nu se execută) = ENFORCEMENT, cu **owner DEFINIT: [NX-188](NX-188.md)** (promovarea QuerySpec din shadow e secțiune explicită acolo; NU există NX-185b). Acest card livrează DOAR detecția.

## Test Cases
**Happy:** „fără parfum, sub 80, ten sensibil" → 3 hard constraints în QuerySpec.
**Failure:** tool-loop cheamă search cu preț mai mare → QuerySpec semnalează disagreement, nu slăbește hard-ul.
