# NX-186 — Typed facet registry + coverage report

**Owner:** Claude (build) / Codex (verify) · **Faza:** P2 · **Track:** Selection Correctness (epic separat) · **Branch:** `feat/NX-186-typed-facets` · **Complexitate:** M/L · **Estimare:** 3-5 zile
**Parent plan:** [docs/RESPONSE-QUALITY-EPIC.md](../docs/RESPONSE-QUALITY-EPIC.md) · **Depinde de:** NX-185 (consumatorul QuerySpec); absoarbe registrul minimal de labels din NX-182

## Goal
Extinde `DomainPack.searchable_facets` (azi doar `tuple[str]`, pack.py:70) la fațete **tipizate** printr-un registru controlat din cod, ȘI produce **raportul de coverage per business + category + facet** — poarta care decide ce fațetă poate ajunge la enforcement (NX-188) și ce fațetă poate fi mutată în SQL (NX-189).

## Business Context
QuerySpec are nevoie de tipuri/operatori ca să verifice constrângeri. SQL-ul actual tratează totul ca array, deși catalogul are scalar/boolean/numeric. Enforcement pe o fațetă fără date = UNKNOWN masiv → recall prăbușit. Deci **coverage-ul se măsoară ÎNAINTE de orice enforcement**, cu praguri **per fațetă**, nu un prag global optimist.

## Contract (per fațetă)
`key` canonic · `value_type` (bool/enum/number/text/list) · `operators` permiși · `values`+`aliases` normalizate · semantica `any/all` · sursa controlată din produs · politica `missing_value` · etichete per-locale (absoarbe registrul minimal din NX-182) · **prag minim de coverage pentru enforcement**.
Config NU poate conține SQL arbitrar / JSON paths interpolate — cheia validată se mapează printr-un **registru din cod** la extractor și (ulterior) la expresia SQL sigură.

## Definiția coverage (contract de măsurare)
Calculat **per business + category + facet**, cu:
- **denominator explicit** (produsele publicate din categoria X ale business-ului Y);
- **număr minim de produse** — sub prag → „date insuficiente", NU coverage optimist;
- trei stări DISTINCTE de provenance: **valoare prezentă** ≠ **valoare validă în registru** ≠ **merchant-verified**;
- distribuția previzionată MATCH/MISMATCH/UNKNOWN pe query-uri reale;
- raportul stabilește **pragurile per fațetă** consumate de NX-188 (enforce) și NX-189 (SQL).

## Technical Description
- `src/domain/facets.py`: registru tipizat + validare la load (DomainPack loader), fail-closed pe config invalid.
- `scripts/facet_coverage.py`: măsurarea de mai sus → `reports/facet-coverage-<business>-<data>.json`.

## Câmpuri TurnContext scrise
Niciunul (config + tooling).

## Implementation Steps
1. Schema registru + tipuri/operatori/missing-policy; absoarbe labels din NX-182.
2. Validare la load (fail-closed pe config invalid).
3. Script coverage per business+category+facet cu denominator + provenance.
4. Praguri documentate per fațetă (input pt NX-188/189).

## Files To Create / Files To Modify
- **Create:** `src/domain/facets.py`, `scripts/facet_coverage.py`.
- **Modify:** `src/domain/pack.py`, `src/domain/loader.py`, `src/domain/defaults/*.json`.

## Database Changes
None (citește catalogul existent).

## Rollout & Kill-switch
Config + tooling — fără kill-switch runtime. Registrul e aditiv (fațete netipizate rămân ca azi până sunt migrate).

## Dependencies
NX-185. Precede NX-187/189.

## Out of Scope
- Aplicarea în Match Gate (NX-187).
- Mutarea în SQL (NX-189).

## Definition of Done
- [x] Registru tipizat validat la load; config invalid → fail-closed, nu SQL arbitrar (`src/domain/facets.py`,
      allowlist sursă + `_KEY_RE`; `tests/test_facets.py` acoperă JSON-path/injection/coloană/operator).
- [x] Raport coverage pentru pilot **per business + category + facet** (`scripts/facet_coverage.py`):
      denominator explicit, min-produse, 3 stări provenance (present≠valid≠verified), unknown_rate +
      value_distribution (→ MATCH/MISMATCH/UNKNOWN). Raport: `reports/facet-coverage-6098812a-2026-07-24.json`.
- [x] Fațetă sub numărul minim de produse (< 5) → „date insuficiente" (nu 100% fals); `enforce_ready=False`.
- [x] Praguri de enforcement documentate **per fațetă** (`min_coverage` în registru + tabel în
      [docs/NX-186-FACET-REGISTRY.md](../docs/NX-186-FACET-REGISTRY.md), coverage real măsurat).
- [x] Labels absorbite într-un singur registru (NX-182 nu a fost construit → labels direct în `TypedFacet.labels`).
- [x] `ruff check .` + `ruff format --check .` + `pytest -x -q` verzi.

**Rezultat cheie:** `price`/`category` 100% coverage+verified (enforce oriunde); `fragrance_free` 40%
coverage & **0% merchant-verified** (claim autorat → soft/disclosure, nu hard — semnal D5 pentru NX-188).
**Notă:** stacked pe branch-ul NX-208 (PR #246, nemerge-uit) — aceleași fișiere `pack.py`/`loader.py`/defaults.

## Test Cases
**Happy:** `fragrance_free` (bool) + `price` (number) + `key_ingredients` (list) validate cu operatorii lor; raport cu denominator + provenance per business+category+facet.
**Failure:** config cu JSON path interpolat → respins la load; fațetă cu 3 produse în categorie → „date insuficiente".
