# T001 — Repo GitHub privat + .gitignore
**Owner:** S · **Faza:** MVP · **Zi:** Z1 · **Branch:** direct pe main (singura excepție — repo gol) · **Complexitate:** XS · **Estimare:** 0.5h

## Goal
Creează fundația de colaborare: repo-ul privat în care trăiește tot proiectul. Există ca task explicit pentru că deciziile de aici (nume, vizibilitate, .gitignore) sunt greu de schimbat după.

## Business Context
Tot codul care va factura retainer-e lunare stă aici. Privat = IP-ul agenției protejat.

## Technical Description
Repo privat `nativx-assistant` în contul/organizația voastră GitHub. `.gitignore` pentru Python: `__pycache__/`, `*.pyc`, `.venv/`, `venv/`, `.env`, `.env.*` (NU `.env.example`), `.pytest_cache/`, `.ruff_cache/`, `htmlcov/`, `.coverage`, `dist/`, `*.egg-info/`. README placeholder (înlocuit de T019). Default branch `main`.

## Principii CLAUDE.md aplicabile
P7 indirect: de aici încolo orice secret în repo e o breșă multi-tenant — `.env` în gitignore din primul commit.

## Implementation Steps
1. `gh repo create nativx-assistant --private --clone`
2. Adaugă `.gitignore` (conținutul de mai sus) + README placeholder.
3. Commit inițial: `chore: init repo` + push.
4. Verifică pe github.com că repo-ul e Private.

## Files To Create
- `.gitignore`
- `README.md` (placeholder)

## Files To Modify
None.

## Database Changes
None.

## API Changes
None.

## Events de emis
None — task de infrastructură, nu produce cod rulabil.

## Dependencies
None — primul task.

## Out of Scope
- Branch protection (T002), CI (T004-T005), structura de foldere (T008).

## Definition of Done
- [ ] Repo privat vizibil pentru ambii membri
- [ ] `git clone` curat funcționează pentru junior
- [ ] `.env` listat în .gitignore (verificat: `git check-ignore .env` → match)

## Test Cases
**Happy Path:** clone + commit + push funcționează pentru ambii. **Edge:** junior are acces de write (nu doar read). **Failure:** `git add .env` → fișierul NU apare în staging.
